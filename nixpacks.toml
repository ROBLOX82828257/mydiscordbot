[phases.setup]
nixPkgs = [
  "nodejs_22",
  "cairo",
  "pango",
  "libpng",
  "libjpeg",
  "giflib",
  "librsvg",
  "fontconfig",
  "pkg-config",
]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "pnpm install --frozen-lockfile",
]

[phases.build]
cmds = []

[start]
cmd = "pnpm --filter @workspace/discord-bot run dev"
